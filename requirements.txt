requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
Pillow>=10.0
numpy>=1.24
imagehash>=4.3
# SVG rasterization — the wiki serves icons as SVG files.
# reportlab 5.0+ requires rlPyCairo as its rendering backend (replaces the
# old bundled _renderPM C extension). rlPyCairo brings pycairo which has
# pre-built Windows wheels, so no system-level Cairo installation is needed.
svglib>=1.5
reportlab>=4.0
rlPyCairo>=0.2
# Optional, only needed for automatic grid detection in vision.py.
# The tool degrades gracefully (manual --grid) if this is missing.
opencv-python-headless>=4.8
