__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Runtime data — the app scrapes the wiki catalog + icons itself at startup,
# or you build it locally with:  python -m app.bootstrap
data/
data_bundle.zip
